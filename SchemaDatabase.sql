create schema sell_clothes;

show databases;

use sell_clothes;

create table users(
	first_name varchar(100),
    last_name varchar(100)
);

create table products(
	name varchar(100),
    price int
);

create database sell_shoes; 

use sell_shoes;


create table users(
	first_name varchar(100),
    last_name varchar(100)
);

create table products(
	name varchar(100),
    price int
);